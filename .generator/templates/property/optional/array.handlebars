with{{Property}}({{Property}}): {
    Properties+::: {
        {{Property}}: (if std.isArray({{Property}}) then {{Property}} else [{{Property}}])
    },
},
with{{Property}}Mixin({{Property}}): {
    Properties+::: {
        {{Property}}+: (if std.isArray({{Property}}) then {{Property}} else [{{Property}}])
    },
},

{{property}}:
    if !std.isNumber({{property}}) then (error '{{property}} must be an number')
{{#if parameters.enum}}
    else if {{#each parameters.enum}} {{../property}} != {{this}}{{#unless @last}} && {{/unless}}{{/each}} then error("{{property}} should be {{#each parameters.enum}}{{this}}{{#unless @last}} or {{/unless}}{{/each}}")
{{/if}}
{{#if parameters.minimum}}
    else if {{property}} < {{parameters.minimum}} then error("{{property}} should be at least {{parameters.minimum}}")
{{/if}}
{{#if parameters.maximum}}
    else if {{property}} > {{parameters.maximum}} then error("{{property}} should be not more than {{parameters.maximum}}")
{{/if}}
    else {{property}},
